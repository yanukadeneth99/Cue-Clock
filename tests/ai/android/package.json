{
  "name": "cueclock-ai-android-runner",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "LangChain.js + LangGraph agent driving agent-device against an Android AVD.",
  "scripts": {
    "_comment": "tsx runs ESM TS directly — no build step. WHY: scenarios iterate fast, type errors surface in dev; production reliability isn't relevant for a local-only harness.",
    "run": "tsx src/runner.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@langchain/core": "^1.1.49",
    "@langchain/google-genai": "^2.1.31",
    "@langchain/langgraph": "^1.4.2",
    "dotenv": "^16.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
